# `wakterm.mux.rename_workspace(old, new)`

{{since('20230408-112425-69ae8472')}}

Renames the workspace *old* to *new*.

```lua
wakterm.mux.rename_workspace(
  wakterm.mux.get_active_workspace(),
  'something different'
)
```
