#!/usr/bin/env bash
set -euo pipefail

if ! command -v cargo >/dev/null 2>&1; then
  exit 0
fi

if ! cargo +nightly fmt --all --check >/dev/null 2>&1; then
  echo "pre-commit: formatting check failed" >&2
  echo "Run: cargo +nightly fmt --all" >&2
  exit 1
fi
